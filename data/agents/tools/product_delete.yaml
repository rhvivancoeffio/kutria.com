name: product_delete
description: Elimina un producto en Gravity Headless (DELETE /Products/{id}).
mcp: true
timeout_ms: 8000
side_effect: true
input_schema:
  type: object
  properties:
    product_id: { type: string }
  required: [product_id]
