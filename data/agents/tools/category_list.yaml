name: category_list
description: Lista o busca categorías en Gravity Headless por nombre.
mcp: true
timeout_ms: 5000
side_effect: false
input_schema:
  type: object
  properties:
    name: { type: string, description: "Filtro por nombre" }
  required: []
