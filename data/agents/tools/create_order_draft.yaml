name: create_order_draft
description: Genera checkout/borrador desde el carrito (alias de cart_checkout). No confirma el pago.
mcp: true
timeout_ms: 8000
side_effect: true
input_schema:
  type: object
  properties:
    cart_id: { type: string }
  required: []
